name: "CI Campaign"
version: "1.0"
profile: ci
target:
  type: rest
  endpoint: http://localhost:9000/invoke
judge:
  provider: keyword
  model: keyword
attacks:
  categories:
    - A
    - F
execution:
  mutation_count: 0
reporting:
  formats: [json]
  output_dir: ./test_output/
